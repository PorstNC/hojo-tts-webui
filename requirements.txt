# Hojo-TTS-Light-40M WebUI — runtime dependencies
# Python >= 3.10
# Models are pre-converted to FP32, so the 'onnx' package is NOT needed at runtime.

numpy>=1.24
soundfile>=0.12
tokenizers>=0.15
flask>=3.0
huggingface_hub>=0.20

# NOTE: onnxruntime is installed by start.sh / start.bat based on your GPU/CPU choice.
#   - CPU:  pip install onnxruntime
#   - GPU:  pip install onnxruntime-gpu
# Do NOT add onnxruntime here directly — the launcher handles it.
